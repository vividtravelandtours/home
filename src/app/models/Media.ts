export interface IMedia {
    status: string,
    updatedAt: string,
    createdAt: string,
    id: string,
    handle: string,
    fileName: string,
    height: Number,
    width: Number,
    size: Number,
    mimeType: string,
}